One-sentence what & when: authored prose fields — weakness `text`, `reachability`, mitigation `rationale`, report `scenario`.

```jsx
<TextArea rows={5} value={w.text} onChange={e => set(e.target.value)} />
```

`resize: vertical` only — horizontal resize would break the grid track.
