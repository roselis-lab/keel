One-sentence what & when: array-of-enum fields — `surface[]` and `source[]` on a threat.

```jsx
<CheckSet options={surfaceEnum} value={t.surface} onToggle={toggleSurface} />
```

Capsules, not a bare checkbox list. `accent-color` is `--crimson-600`; hover only moves the border to `--crimson-200`.
