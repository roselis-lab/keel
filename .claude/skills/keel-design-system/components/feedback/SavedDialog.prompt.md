One-sentence what & when: after any successful write, to name the YAML file the user now needs to commit.

```jsx
<SavedDialog file="catalog/threats/T-TOOL-ABUSE.yaml" repoUrl={cfg.repo_url} onClose={hide} />
```

The green `4px` left border is one of only two left-accent borders in the product — do not generalise it.
