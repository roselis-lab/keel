One-sentence what & when: any pane with nothing selected or nothing matching.

```jsx
<EmptyState>Select a threat from the list.</EmptyState>
<EmptyState top="40px">No matches.</EmptyState>
```

Do not add imagery, and do not add a button — the affordance is already in the rail.
