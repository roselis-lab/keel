One-sentence what & when: inside the read view's History accordion, or behind "view change" on the provenance line.

```jsx
<DiffView file="catalog/threats/T-TOOL-ABUSE.yaml" patch={diff.patch} />
```

Every write in Keel is a YAML diff, so this is the product's proof-of-change surface. Do not restyle it into Keel's palette.
