One-sentence what & when: the Overview's per-entity coverage list and the style-guide field tree.

```jsx
<CoverageBar label="threat.reachability" percent={92} />
<CoverageBar label="threat.mitre_id" percent={0} orphan />
```

Do not choose the colour yourself — the thresholds are fixed so coverage cannot be flattered.
