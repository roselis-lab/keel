One-sentence what & when: the Overview's headline counts, in a flex row with 12px gaps.

```jsx
<div style={{ display: "flex", gap: 12 }}>
  <StatTile value={13} label="threats" />
  <StatTile value={71} label="mitigations" />
</div>
```
