One-sentence what & when: any repeated record inside a section band — a weakness, a mitigation link, a report finding.

```jsx
<Card id="CTRL-TOOL-ALLOWLIST" title="Tool allowlist" jump onClick={go}
  badges={<Badge tone="harm">gating</Badge>}
  rationale="An allowlist limits exposure of destructive operations to the tools the agent actually needs" />
```

Set `jump` only when the card navigates. Non-interactive cards must not have a hover state.
