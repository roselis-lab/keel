One-sentence what & when: orients a read view inside the model and shows when it last changed.

```jsx
<Breadcrumb type="Threats" id="T-TOOL-ABUSE" onType={backToList}
  lastChanged="Last changed 2 days ago by jane" onViewChange={showDiff} />
```

Read views only — the edit form drops it.
