One-sentence what & when: every row of the threats, mitigations, style-guide and reports rails.

```jsx
<RailRow id="T-TOOL-ABUSE" title="Unauthorized or destructive tool action" selected onClick={select} />
```

Title is the primary line, id the secondary — never the other way round.
