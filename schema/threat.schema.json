{
  "$defs": {
    "MitigationLink": {
      "additionalProperties": false,
      "description": "Link to a mitigation card, with its role for this threat.",
      "properties": {
        "exception": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Rare, narrow carve-out where this control doesn't apply to this threat (the threat itself stays live) \u2014 NOT a restatement of reachability",
          "title": "Exception"
        },
        "id": {
          "description": "A real CTRL-* mitigation id",
          "title": "Id",
          "type": "string"
        },
        "rationale": {
          "title": "Rationale",
          "type": "string"
        },
        "strength": {
          "description": "gating (blocks) | soft (only lowers likelihood)",
          "enum": [
            "gating",
            "soft"
          ],
          "title": "Strength",
          "type": "string"
        }
      },
      "required": [
        "id",
        "strength",
        "rationale"
      ],
      "title": "MitigationLink",
      "type": "object"
    },
    "Reference": {
      "additionalProperties": false,
      "description": "Evidence for the threat: an incident, a paper, an advisory.\n\nNOT a framework mapping. Which OWASP or ATLAS entries this threat answers is\nrecorded once in `catalog/coverage/` and read back from there, so a reference\nthat only says \"this is LLM01\" is a duplicate of the coverage matrix.",
      "properties": {
        "note": {
          "description": "One line: what this source actually supports. A bare link asks every later reader to re-derive why it was worth citing",
          "title": "Note",
          "type": "string"
        },
        "title": {
          "title": "Title",
          "type": "string"
        },
        "url": {
          "format": "uri",
          "maxLength": 2083,
          "minLength": 1,
          "title": "Url",
          "type": "string"
        }
      },
      "required": [
        "title",
        "url",
        "note"
      ],
      "title": "Reference",
      "type": "object"
    },
    "Weakness": {
      "additionalProperties": false,
      "description": "An architectural predisposing condition on a component.",
      "properties": {
        "component": {
          "enum": [
            "model",
            "tool",
            "downstream",
            "memory",
            "knowledge-base",
            "identity-store"
          ],
          "title": "Component",
          "type": "string"
        },
        "nature": {
          "default": "targeted",
          "enum": [
            "targeted",
            "secondary"
          ],
          "title": "Nature",
          "type": "string"
        },
        "surface": {
          "description": "The channels this weakness is reached through, when it is about content crossing into the component. A list, because one condition is often reachable over several channels and one control closes it on all of them - splitting it per channel would make three weaknesses of one. Empty for a weakness about the component's own authority, which nothing has to flow in to exploit",
          "items": {
            "enum": [
              "user-input",
              "retrieved-content",
              "tool-output",
              "agent-message",
              "memory",
              "training-data",
              "model-output"
            ],
            "type": "string"
          },
          "title": "Surface",
          "type": "array"
        },
        "text": {
          "description": "Architectural condition: cause + where + defect",
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "component",
        "text"
      ],
      "title": "Weakness",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Create payload \u2014 same shape, id required.",
  "properties": {
    "harm": {
      "enum": [
        "wrong-decision",
        "data-integrity",
        "data-exposed",
        "code-execution",
        "downtime",
        "reputation-legal"
      ],
      "title": "Harm",
      "type": "string"
    },
    "id": {
      "description": "Letters, digits, dot, hyphen and underscore. It is also the file name, so anything that could be read as a path is refused",
      "title": "Id",
      "type": "string"
    },
    "mitigations": {
      "items": {
        "$ref": "#/$defs/MitigationLink"
      },
      "title": "Mitigations",
      "type": "array"
    },
    "positioning": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How this entry sits relative to the entries in the sources Keel tracks - only what the mapping itself does not already say. Where a source's entry is broader than this one, say what the rest of it is and where that lives. Never catalog state (which is computed) and never a to-do",
      "title": "Positioning"
    },
    "reachability": {
      "description": "Rule-out gate: the condition under which this threat is not a live path at all, judged on the un-mitigated architecture",
      "title": "Reachability",
      "type": "string"
    },
    "references": {
      "items": {
        "$ref": "#/$defs/Reference"
      },
      "title": "References",
      "type": "array"
    },
    "source": {
      "items": {
        "enum": [
          "external-attacker",
          "internal",
          "hallucination",
          "error",
          "accident"
        ],
        "type": "string"
      },
      "title": "Source",
      "type": "array"
    },
    "tags": {
      "items": {
        "type": "string"
      },
      "title": "Tags",
      "type": "array"
    },
    "title": {
      "title": "Title",
      "type": "string"
    },
    "weaknesses": {
      "items": {
        "$ref": "#/$defs/Weakness"
      },
      "minItems": 1,
      "title": "Weaknesses",
      "type": "array"
    }
  },
  "required": [
    "id",
    "title",
    "harm",
    "weaknesses",
    "reachability"
  ],
  "title": "ThreatCreate",
  "type": "object"
}
